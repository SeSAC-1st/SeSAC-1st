<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>사용자 게시글 조회 페이지</title>
    <%- include('../include/head.ejs') %>
  </head>
  <body>
    <%- include('../include/header.ejs') %>
    <main class="px-4 sm:px-6 lg:px-52 pt-20">
      <% if(userPostList.count > 0) { %>
      <article
        class="flex flex-col self-stretch my-auto max-md:mt-5 max-md:max-w-full"
      >
        <div
          class="justify-center items-center px-16 py-14 text-5xl text-center text-black bg-zinc-300 max-md:px-5 max-md:max-w-full max-md:text-4xl"
        >
          월별 게시물 수 차트
        </div>

        <!-- 게시글 -->
        <!-- 외부 그리드 -->
        <div
          class="mt-6 grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:gap-x-8"
        >
          <!-- 내부 그리드 -->
          <%- include('../include/postgrid.ejs', { userPostList: userPostList })
          %>
        </div>

        <%- include('../include/pagination.ejs') %>
      </article>
      <% } else { %>
      <article>
        <div>게시글이 없습니다.</div>
        <button>글 작성하기</button>
      </article>
      <% } %>
    </main>
    <%- include('../include/footer.ejs') %>
  </body>
</html>
